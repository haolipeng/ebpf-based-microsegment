server:
  host: "0.0.0.0"
  port: 8080

grpc:
  host: "0.0.0.0"
  port: 9090

database:
  host: "10.107.12.201"
  port: 5432
  user: "microsegment_user"
  password: "secret"
  dbname: "microsegment"
  sslmode: "disable"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: "5m"

log:
  level: "info"  # debug, info, warn, error
  format: "json"  # json, text
